---
modules:
  - type: dnf
    install:
      packages:
        - alacritty
        - emacs
        - zathura
        - zathura-cb
        - zathura-ps
        - zathura-djvu
        - zathura-pdf-poppler

  - type: default-flatpaks
    configurations:
      - notify: true
        install:
          - io.gitlab.librewolf-community

  # - type: script
  #   snippets:
  #     - "dnf -y builddep emacs"

  - type: justfiles
    validate: true
    include:
      - syke.just

  - type: signing
