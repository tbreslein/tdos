---
modules:
  - type: dnf
    # repos:
    #   cleanup: true
    #   nonfree: rpmfusion

    install:
      packages:
        - alacritty
        - emacs
        - libvterm
        - zathura
        - zathura-cb
        - zathura-ps
        - zathura-djvu
        - zathura-pdf-poppler

        # - qutebrowser
        # - ffmpeg # already installed on both bazzite and bluefin

  - type: default-flatpaks
    configurations:
      - notify: false
        install:
          - io.gitlab.librewolf-community

  - type: justfiles
    validate: true
    include:
      - syke.just

  - type: signing
