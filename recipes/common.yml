---
modules:
  - type: dnf
    repos:
      cleanup: true

    install:
      packages:
        - alacritty
        - emacs
        - libvterm
        - zathura
        - zathura-cb
        - zathura-ps
        - zathura-djvu
        - zathura-pdf-poppler

        - qutebrowser
        - libavcodec-freeworld

  - type: default-flatpaks
    configurations:
      - notify: false
        install:
          - io.gitlab.librewolf-community

  - type: justfiles
    validate: true
    include:
      - syke.just

  - type: signing
